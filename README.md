# shorty
A simple private url shortener using Node.js &amp; MySQL

## development

```bash
$ mkdir -p public/css
$ npm install
$ npm i -g nodemon
$ npm run sass:watch
$ npm run server:stat:dev
```

## production

```bash
$ mkdir -p public/css
$ npm install
$ npm run sass:build
$ npm run server:start
```
